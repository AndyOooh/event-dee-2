{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "lint": {},
    "dev": {
      "env": ["NODE_ENV"],
      "cache": false,
      "persistent": true
    },
    "clean": {
      "cache": false
    }
  }
}

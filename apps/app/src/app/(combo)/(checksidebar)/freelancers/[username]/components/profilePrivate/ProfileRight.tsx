import React from 'react';
import { ColoredDiv } from 'ui';

type Props = {};

export const ProfileRight = (props: Props) => {
  return (
    <div className='flex flex-1 flex-col gap-4 mt-16'>
      {/* <ColoredDiv color='white' direction='top' height={6} className='z-10' />
      <ColoredDiv color='purple' direction='top' height={6} className='z-10' />
      <ColoredDiv color='blue' direction='top' height={6} className='z-10' /> */}
    </div>
  );
};

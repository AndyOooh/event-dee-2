import { ComingSoon } from '@repo/ui';
import { container } from '../../styles/styles';

export default function SocietyPage() {
  return (
    <div>
      <ComingSoon pageName="Society" containerClass={container} />
    </div>
  );
}

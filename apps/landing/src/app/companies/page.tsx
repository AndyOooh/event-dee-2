import { ComingSoon } from 'ui';
import { container } from '../../styles/styles';

export default function CompaniesPage() {
  return (
    <div>
      <ComingSoon pageName='Companies' containerClass={container} />
    </div>
  );
}
